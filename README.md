# Vue3 Admin System

一个基于 Vue3 + Element Plus + Vite 构建的现代化后台管理系统。

## 技术栈

- **框架**: Vue 3.3+ (Composition API)
- **构建工具**: Vite 5.0+
- **UI 组件库**: Element Plus 2.4+
- **状态管理**: Pinia 2.1+
- **路由**: Vue Router 4.2+
- **HTTP 客户端**: Axios 1.6+
- **图表库**: ECharts 5.4+
- **语言**: TypeScript 5.2+
- **包管理器**: pnpm
- **代码规范**: ESLint + Prettier + Stylelint

## 功能特性

### 🔐 认证授权
- 用户登录/登出
- Token 自动刷新
- 记住我功能
- 路由白名单
- 未登录自动重定向

### 🛡️ 权限管理
- 基于角色的路由过滤
- 基于权限的菜单过滤
- 指令级别的按钮权限控制
- 组件级别的权限控制

### 🎨 界面布局
- 响应式布局设计
- 侧边栏折叠/展开
- 标签页导航（可选）
- 面包屑导航（可选）
- 主题切换（明暗模式）
- 全屏功能

### 📊 数据可视化
- ECharts 图表集成
- 响应式图表
- 多种图表类型支持

### 🔧 开发体验
- TypeScript 类型支持
- 自动导入配置
- 热重载开发
- 代码格式化
- Git 提交规范

## 项目结构

```
src/
├── api/                 # API 接口
├── components/          # 公共组件
│   └── Layout/         # 布局组件
├── directives/         # 自定义指令
├── composables/              # 组合式函数
├── router/             # 路由配置
├── store/              # 状态管理
│   └── modules/        # 状态模块
├── styles/             # 样式文件
├── types/              # 类型定义
├── utils/              # 工具函数
└── views/              # 页面组件
    ├── dashboard/      # 首页
    ├── error/          # 错误页面
    └── login/          # 登录页面
```

## 快速开始

### 环境要求

- Node.js >= 20.x
- pnpm >= 8.x

### 安装依赖

```bash
pnpm install
```

### 启动开发服务器

```bash
pnpm dev
```

### 构建生产版本

```bash
pnpm build
```

### 代码检查

```bash
# ESLint 检查
pnpm lint

# 代码格式化
pnpm format

# 样式检查
pnpm lint:style
```

## 默认账号

- 用户名: `admin`
- 密码: `123456`

## 浏览器支持

- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 许可证

MIT License